[project]
name = "hackerrank-api"
version = "0.1.0"
description = "A FastAPI HackerRank port of the LeetCode Stats API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.12",
    "httpx>=0.28.1",
    "redis>=5.2.0",
    "uvicorn>=0.34.2",
]
